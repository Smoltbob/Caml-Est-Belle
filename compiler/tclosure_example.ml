let rec gamma w =
    5
in
let rec delta o =
    let rec epsilon p =
        6
    in
    7
in
let rec zeta q =
    let y = 8 in
    let rec eta r =
        9
    in
    let z = 10 in
    let rec theta s =
        let a = 11 in
        12
    in
    13
in
let x =
    let rec alpha u = 
        let y = 
            let rec beta v =
                1
            in
            2
        in
        3
    in
    4
in
print_int 42
